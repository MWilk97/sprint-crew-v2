[project]
name = "sprint-crew-v2"
version = "0.1.0"
description = "Sprint crew agents: Pydantic AI + LangGraph + vLLM"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Maciej Wilk", email = "maciej.wilk97@gmail.com" }]
keywords = ["llm", "agents", "langgraph", "pydantic-ai", "vllm", "sprint-automation"]
urls = { Repository = "https://github.com/MWilk97/sprint-crew-v2" }
classifiers = [
    "Programming Language :: Python :: 3.12",
    "License :: OSI Approved :: MIT License",
    "Framework :: Pydantic :: 2",
    "Topic :: Software Development :: Code Generators",
]
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic>=2.13",
    "pydantic-settings",
    "pydantic-ai[openai]",
    "langgraph",
    "langgraph-checkpoint-sqlite",
    "aiosqlite",
    "openai",
    "httpx",
    "atlassian-python-api",
    "PyGithub",
    "pyyaml",
    "qdrant-client",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "pytest-timeout",
    "ruff",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sprint_crew"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "agent_live: e2e tests on fixture repo against real vLLM lanes (opt-in VLLM_LIVE=1)",
    "vllm_live: real vLLM lanes (VLLM_LIVE=1)",
    "vector_agent_live: full sprint cycle with vector index on (VECTOR_AGENT_LIVE=1 + VLLM_LIVE=1)",
    "agent_trap: adversarial fixtures — stdlib shadow, decoys",
]
timeout = 1200

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests", "scripts"]
# Sample/target repositories the agent operates on — not linted as our source.
extend-exclude = ["fixtures"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501", "UP042", "UP047"]
